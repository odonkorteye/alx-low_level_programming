#include <stdio.h>
/**
 * main - Prints "Programming is like building a multiligual puzzle"
 * main: prints a string output using puts
 *
 * Return: always 0 as success
 */
int main(void)
	{
		puts("\"Programming is like building a multiligual puzzle\n");
		return (0);
	}
